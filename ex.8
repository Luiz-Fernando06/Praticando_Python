#desconto
produto = float(input('digite o preço do produto: '))
desconto = produto * 0.05
preco_desconto = produto - desconto
print("O desconto do produto foi de {:.2f} reais".format(preco_desconto))
